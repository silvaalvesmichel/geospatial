spring.application.name=geospatial

# Configuracao PostgreSQL prod
#spring.datasource.url=jdbc:postgresql://localhost:5432/geospatial_db
#spring.datasource.username=postgres
#spring.datasource.password=sua_senha
# Especificar o driver explicitamente ajuda o Spring em alguns contextos de inicializacao
#spring.datasource.driver-class-name=org.postgresql.Driver

# Configuracao H2 em memoria
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=false

# Configuracao JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
# Exibe o SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuracao Swagger
springdoc.swagger-ui.path=/swagger-ui.html